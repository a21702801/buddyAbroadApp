export interface RegisterUser {
    first_name: string;
    last_name: string;
    gernder: string;
    dateOfBirth: Date;
    email: string;
    password: string;
}

export interface PalavraEmJson {
    info: string;
}
